# 1G1R Deployment Tool


This is a Python module for converting emulation ROMs and Disk Images from archival formats to emulator-friendly formats.

## Usage

Install dependencies and run the CLI:

```sh
python -m src.onegame_onerom.cli --source <input_dir> --config ./config.yaml
```

## Configuration
See `config.yaml` for an example YAML config.
